# Server configuration
PORT=5000
NODE_ENV=development

# Database connection URI (falls back to In-Memory MongoDB if unavailable)
MONGODB_URI=mongodb://127.0.0.1:27017/expojudge

# Secret key for JWT signing & verification
JWT_SECRET=expojudge_secure_jwt_secret_key_2026
